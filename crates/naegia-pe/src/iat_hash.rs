//! ROR13 uppercase import hashing (removed from production).
//! The hash_name_ror13_upper function was previously here but was dead code:
//! no production consumer existed. The algorithm is well-known (ROR13 + ASCII uppercase).
//! If re-added, see git history for the 5-line implementation.
